# Toolify Vercel deployment environment example
# Copy this file to .env for local development only.
# Do NOT commit real secrets.

# Base64-encoded YAML config content (recommended for Vercel)
# Generate with: base64 -w0 config.yaml
TOOLIFY_CONFIG_B64=

# Optional: runtime env
PYTHONUNBUFFERED=1
NODE_ENV=production
